@model IEnumerable<_2010107281_ENT.Entities.TipoComprobante>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ComprobantePago.ComprobantePagoId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ComprobantePago.ComprobantePagoId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoComprobanteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoComprobanteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoComprobanteId })
        </td>
    </tr>
}

</table>

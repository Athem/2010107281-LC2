@model IEnumerable<_2010107281_ENT.Entities.ServicioHospedaje>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HospedajeId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HospedajeId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ServicioHospedajeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ServicioHospedajeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ServicioHospedajeId })
        </td>
    </tr>
}

</table>
